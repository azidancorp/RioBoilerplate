# Secrets only.
# See app/app/config.py for non-secret behavior and provider defaults.

# Required for Google OAuth. Generate once with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Required when EMAIL_METHOD = "resend" in app/app/config.py.
# RESEND_API_KEY=
# Required when EMAIL_METHOD = "smtp" and SMTP_USERNAME is configured.
# RIO_SMTP_PASSWORD=
# RIO_CONTACT_NTFY_CHANNEL=
